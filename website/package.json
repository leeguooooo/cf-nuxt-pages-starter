{
  "name": "cf-nuxt-pages-kit-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "wrangler:config": "node scripts/use-pages-config.mjs",
    "deploy": "pnpm wrangler:config && pnpm build && wrangler pages deploy dist --project-name cf-nuxt-pages-kit-site"
  },
  "dependencies": {
    "@nuxt/ui": "^2.15.0",
    "nuxt": "^4.2.0"
  },
  "devDependencies": {
    "wrangler": "^4.45.3"
  }
}
